/// <reference types="Cypress" />

export const enum NameTask{
    Javascript = 'Learn Javascript',
    React = 'Learn React',
    Build = 'Build a React App',
    New = 'New Task',
    Alert = 'There are no items.'
}

export const enum State {
    completed = 'completed',
    pending = 'pending'
}

export const enum FiltrationBtn {
    all = 'All',
    active = 'Active',
    completed = 'Completed'
}
